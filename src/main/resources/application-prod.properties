# Prod profile defaults
# Larger upload limits for production workloads (tune to your needs)
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
server.port=8080
logging.level.com.pdf.table.extractor=INFO
